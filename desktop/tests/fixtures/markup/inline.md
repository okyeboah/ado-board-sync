This paragraph has **bold**, *italics*, and `inline code`.

A backlog says `SharedKernel.*` and `*Command` in one line, which the italics
pass must not reach into.

Ampersands & angle brackets <like this> are escaped before any markdown runs.

**Bold with `code` inside** and `code with **bold** inside`.

A line ending in an unpaired * asterisk.
